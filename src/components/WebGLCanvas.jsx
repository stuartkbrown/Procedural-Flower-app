import React from "react";

const WebGLCanvas = () => {
  return (
    <canvas
      class="webgl"
      className="absolute top-0 left-0 z-10 w-full h-full"
    ></canvas>
  );
};

export default WebGLCanvas;
